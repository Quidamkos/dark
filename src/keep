<div>
    <input type="checkbox" onClick={repeat}/>
    <span>Répétition</span>
    {showRepeat ? (
        <div>
        <input type="checkbox"/>
        <span>Période type</span>
        <div>
            <span>Tous les :</span>
            <input 
            type="number"
            name='repeatPeriod' 
            value={taskDetails.repeatPeriod} 
            onChange={handleInputChange}
            className="text-input" 
            placeholder="Number"
            />
            <select>
            <option value="day">Jour</option>
            <option value="week">Semaine</option>
            <option value="month">Mois</option>
            <option value="year">Année</option>
            </select>
        </div>
        <div>
            <span>Semaine :</span>
            <input type="checkbox"/>
            <input type="checkbox"/>
            <input type="checkbox"/>
            <input type="checkbox"/>
            <input type="checkbox"/>
            <input type="checkbox"/>
            <input type="checkbox"/>
        </div>
        </div>
    ) : (
        ""
    )}
</div>